<li>
  <%# link_to resume.title, resume_path(id: resume.id) %> <%# 可簡化為下方 %>
  <%= "V" if resume.pinned %>
  <%= link_to resume.title, user_resume_path(user_id: resume.user.username, id: resume.slug ) %>
  |
  <%= link_to '預設', pin_resume_path(resume), method: :patch %>
  |
  <%= link_to 'X', resume_path(resume), method: "delete", data:{confirm: "確定刪除嗎？"} %>
</li>